#include "toados_globals.h"

using namespace std;
/*
bool ts_load_font(int family, char* path, int small, int med, int lg) {

	// @todo: make sure font_addon was loaded and return false if any of these fonts don't load
	font[family][TS_FONT_SIZE_S] = al_load_font(path, small, 0);
	font[family][TS_FONT_SIZE_M] = al_load_font(path, med, 0);
	font[family][TS_FONT_SIZE_L] = al_load_font(path, lg, 0);

	return true;
}
*/
